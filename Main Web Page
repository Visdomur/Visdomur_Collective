<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>System Profile</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,600;1,400&family=Inconsolata:wght@300;400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    /* ══════════════════════════════════════════
       TOKENS
    ══════════════════════════════════════════ */
    :root {
      --bg:        #f2ede6;
      --bg2:       #e8e1d6;
      --bg3:       #ddd5c8;
      --card:      #faf8f4;
      --accent:    #7a8c6e;
      --accent2:   #a3b48f;
      --accent3:   #4e5e44;
      --warm:      #c4956a;
      --warm2:     #deb896;
      --text:      #2b2318;
      --muted:     #7a6e61;
      --border:    rgba(122,140,110,0.22);
      --shadow:    4px 4px 0px rgba(78,94,68,0.28);
      --radius:    16px;
      --font-head: 'Lora', Georgia, serif;
      --font-body: 'Inconsolata', monospace;
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      background-color: var(--bg);
      background-image:
        radial-gradient(ellipse at 15% 20%, rgba(163,180,143,0.18) 0%, transparent 55%),
        radial-gradient(ellipse at 85% 75%, rgba(196,149,106,0.13) 0%, transparent 50%);
      font-family: var(--font-body);
      color: var(--text);
      min-height: 100vh;
    }

    /* ══════════════════════════════════════════
       BANNER
    ══════════════════════════════════════════ */
    .banner {
      width: 100%; height: 200px;
      background: linear-gradient(135deg,#c8d4b8 0%,#a3b48f 45%,#7a8c6e 100%);
      position: relative; overflow: hidden;
    }
    .banner::before {
      content: ''; position: absolute; inset: 0;
      background-image:
        radial-gradient(circle at 20% 50%,rgba(255,255,255,.08) 0%,transparent 40%),
        radial-gradient(circle at 80% 30%,rgba(196,149,106,.15) 0%,transparent 35%);
    }
    #bannerImg { width: 100%; height: 100%; background-size: cover; background-position: center; }

    /* ══════════════════════════════════════════
       PAGE LAYOUT
    ══════════════════════════════════════════ */
    .page-wrap { max-width: 1180px; margin: 0 auto; padding: 0 18px 80px; }
    .main-grid {
      display: grid;
      grid-template-columns: 240px 1fr 200px;
      gap: 18px;
      align-items: start;
    }

    /* ══════════════════════════════════════════
       SHARED
    ══════════════════════════════════════════ */
    .card { background: var(--card); border-radius: var(--radius); box-shadow: var(--shadow); border: 1px solid var(--border); }
    .divider { border: none; border-top: 1px dashed var(--border); margin: 22px 0; }
    .prose { font-size: .82rem; line-height: 1.85; color: var(--text); margin-bottom: 14px; }

    /* ══════════════════════════════════════════
       LEFT SIDEBAR
    ══════════════════════════════════════════ */
    .sidebar-left { position: sticky; top: 18px; }
    .avatar-wrap { display: flex; justify-content: center; margin-top: -65px; margin-bottom: 14px; }
    #avatarEl {
      width: 130px; height: 130px; border-radius: 50%;
      border: 4px solid var(--card);
      box-shadow: 0 0 0 3px var(--accent), var(--shadow);
      background: var(--bg2) center/cover no-repeat;
      display: flex; align-items: center; justify-content: center;
      font-size: 2.8rem; color: var(--accent);
      overflow: hidden; flex-shrink: 0;
    }
    #avatarEl img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
    .left-inner { padding: 18px; }
    .username { font-family: var(--font-head); font-size: 1.75rem; text-align: center; line-height: 1.15; }
    .username em { font-style: italic; color: var(--accent3); }
    .tagline { text-align: center; font-size: .68rem; letter-spacing: 2px; text-transform: uppercase; color: var(--muted); margin: 5px 0 18px; }
    .slabel { font-size: .6rem; letter-spacing: 2.5px; text-transform: uppercase; color: var(--muted); margin: 14px 0 6px; display: flex; align-items: center; gap: 6px; }
    .slabel::after { content: ''; flex: 1; height: 1px; background: var(--border); }
    .nav-list { list-style: none; display: flex; flex-direction: column; gap: 6px; }
    .nav-list a { display: flex; align-items: center; justify-content: space-between; gap: 8px; text-decoration: none; color: var(--text); font-size: .74rem; letter-spacing: .5px; padding: 8px 12px; border-radius: 10px; border: 1px solid transparent; transition: background .15s, border-color .15s, color .15s; }
    .nav-list a:hover { background: var(--bg2); border-color: var(--border); color: var(--accent3); }
    .nav-list .nav-icon { color: var(--accent); width: 16px; text-align: center; }
    .nav-list .nav-arrow { color: var(--muted); font-size: .55rem; }
    .social-list { list-style: none; display: flex; flex-direction: column; gap: 5px; }
    .social-list a { display: flex; align-items: center; gap: 8px; text-decoration: none; color: var(--text); font-size: .72rem; padding: 5px 10px; border-radius: 8px; transition: background .15s, color .15s; }
    .social-list a:hover { background: var(--bg2); color: var(--accent3); }
    .social-list a i { color: var(--warm); width: 15px; text-align: center; }

    /* ══════════════════════════════════════════
       CENTER SCROLL BOX
    ══════════════════════════════════════════ */
    .scroll-card {
      background: var(--card); border-radius: var(--radius);
      box-shadow: var(--shadow); border: 1px solid var(--border);
      height: 680px; overflow-y: auto;
      scrollbar-width: thin; scrollbar-color: var(--accent2) transparent;
      display: flex; flex-direction: column;
    }
    .scroll-card::-webkit-scrollbar { width: 5px; }
    .scroll-card::-webkit-scrollbar-thumb { background: var(--accent2); border-radius: 10px; }

    /* music bar */
    .music-bar {
      background: var(--text); color: var(--card);
      font-size: .67rem; letter-spacing: 1.5px; text-transform: uppercase;
      padding: 8px 16px; border-radius: var(--radius) var(--radius) 0 0;
      display: flex; align-items: center; gap: 10px; overflow: hidden;
      flex-shrink: 0; position: sticky; top: 0; z-index: 10;
    }
    .music-bar i { color: var(--accent2); flex-shrink: 0; }
    .ticker-wrap { overflow: hidden; flex: 1; }
    .ticker-wrap span { display: inline-block; animation: ticker 20s linear infinite; white-space: nowrap; }
    @keyframes ticker { 0%{transform:translateX(60%)} 100%{transform:translateX(-100%)} }
    .yt-embed { position: absolute; opacity:.0001; width:1px; height:1px; pointer-events:none; }

    .scroll-inner { padding: 22px 26px; flex: 1; }

    /* section header bar */
    .sec-head {
      display: inline-flex; align-items: center; gap: 9px;
      font-family: var(--font-head); font-size: 1.35rem; font-style: italic;
      color: var(--card); background: var(--accent3);
      padding: 4px 18px 4px 14px;
      border-radius: 0 var(--radius) var(--radius) 0;
      margin-left: -26px; margin-bottom: 16px;
      box-shadow: 3px 3px 0 var(--accent);
      transition: opacity .2s;
    }
    .sec-head i { font-size: .9rem; }
    .sec-head.hidden-head { opacity: 0; pointer-events: none; height: 0; margin: 0; overflow: hidden; padding: 0; box-shadow: none; }

    /* ── TIMEZONE ── */
    .tz-widget {
      display: flex; align-items: center; gap: 14px;
      background: var(--bg2); border-radius: 10px;
      padding: 12px 20px; margin-bottom: 18px;
      border: 1px solid var(--border); width: 100%;
    }
    .tz-widget i { color: var(--accent3); font-size: 1.2rem; flex-shrink: 0; }
    .tz-right { flex: 1; }
    .tz-label { font-size: .6rem; text-transform: uppercase; letter-spacing: 2px; color: var(--muted); margin-bottom: 2px; }
    #tzName { font-size: .72rem; color: var(--text); margin-bottom: 3px; }
    #tzClock { font-family: var(--font-head); font-size: 1.8rem; color: var(--accent3); letter-spacing: 3px; line-height: 1; }

    /* ── SLIDESHOW ── */
    .slideshow {
      position: relative; border-radius: var(--radius);
      overflow: hidden; height: 220px;
      background: var(--bg3); margin-bottom: 8px;
      border: 1px solid var(--border);
    }
    .slide {
      position: absolute; inset: 0;
      background-size: cover; background-position: center;
      opacity: 0; transition: opacity 1.2s ease;
      display: flex; align-items: flex-end;
    }
    .slide.active { opacity: 1; }
    .slide-caption { background: linear-gradient(transparent,rgba(43,35,24,.65)); color: #fff; font-size: .65rem; letter-spacing: 2px; text-transform: uppercase; padding: 20px 14px 10px; width: 100%; }
    .slide-ph { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8px; color: var(--muted); font-size: .75rem; letter-spacing: 1px; }
    .slide-ph i { font-size: 2rem; color: var(--bg3); }
    .slide-dots { display: flex; gap: 6px; justify-content: center; padding: 8px 0 4px; }
    .dot { width:8px; height:8px; border-radius:50%; background:var(--bg3); border:1px solid var(--border); cursor:pointer; transition:background .2s; }
    .dot.active { background: var(--accent); }

    /* ── FRONTER PROFILE GRID ── */
    .fronter-grid { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 14px; }
    .fronter-card {
      display: flex; flex-direction: column; align-items: center; gap: 6px;
      background: var(--bg2); border-radius: 12px;
      padding: 10px 10px 8px;
      border: 1px solid var(--border);
      width: 90px;
      text-decoration: none; color: var(--text);
      transition: box-shadow .2s, transform .2s;
      position: relative;
    }
    .fronter-card:hover { transform: translateY(-2px); box-shadow: 3px 3px 0 rgba(78,94,68,.25); }
    .fronter-thumb {
      width: 58px; height: 58px; border-radius: 50%;
      background: var(--bg3); background-size: cover; background-position: center;
      border: 2px solid var(--accent);
      display: flex; align-items: center; justify-content: center;
      font-size: 1.4rem; color: var(--muted); flex-shrink: 0;
      overflow: hidden;
    }
    .fronter-thumb img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
    .fronter-name { font-size: .65rem; text-transform: uppercase; letter-spacing: 1px; text-align: center; color: var(--text); line-height: 1.3; }
    .fronter-add {
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      gap: 4px; background: transparent;
      border: 1px dashed var(--border);
      cursor: pointer; font-family: var(--font-body);
      color: var(--muted); font-size: .6rem; letter-spacing: 1px;
    }
    .fronter-add i { font-size: 1.2rem; color: var(--accent2); }
    .fronter-add:hover { background: var(--bg2); color: var(--accent3); }

    /* ── DISCLAIMER ── */
    .disclaimer-box { background: var(--bg2); border-radius: 10px; padding: 14px 18px; font-size: .8rem; line-height: 1.8; border-left: 3px solid var(--warm); }
    .disclaimer-box ul { padding-left: 18px; }
    .disclaimer-box li { margin-bottom: 4px; }

    /* ── CUSTOM SECTIONS ── */
    .custom-section { margin-bottom: 0; }
    .custom-section .sec-block-inner { margin-bottom: 0; }
    .custom-link-list { list-style: none; display: flex; flex-direction: column; gap: 6px; }
    .custom-link-list a { display: flex; align-items: center; gap: 8px; text-decoration: none; color: var(--text); font-size: .78rem; padding: 7px 12px; border-radius: 8px; background: var(--bg2); border: 1px solid var(--border); transition: background .15s; }
    .custom-link-list a:hover { background: var(--bg3); color: var(--accent3); }
    .custom-link-list a i { color: var(--accent); }
    .custom-img-block { border-radius: var(--radius); overflow: hidden; border: 1px solid var(--border); margin-bottom: 8px; }
    .custom-img-block img { width: 100%; display: block; }
    .custom-img-caption { font-size: .7rem; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); padding: 6px 12px; background: var(--bg2); }

    /* ══════════════════════════════════════════
       RIGHT SIDEBAR — character cards
    ══════════════════════════════════════════ */
    .sidebar-right { position: sticky; top: 18px; display: flex; flex-direction: column; gap: 14px; }
    .char-card { background: var(--card); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow); overflow: visible; position: relative; transition: box-shadow .2s; }
    .char-thumb { width: 100%; aspect-ratio: 4/3; background: var(--bg2); background-size: cover; background-position: center; border-radius: var(--radius) var(--radius) 0 0; position: relative; display: flex; align-items: center; justify-content: center; color: var(--muted); font-size: 2.2rem; overflow: hidden; }
    .char-thumb-overlay { position: absolute; inset: 0; background: linear-gradient(transparent 40%,rgba(43,35,24,.6)); }
    .char-thumb-name { position: absolute; bottom: 0; left: 0; right: 0; color: #fff; font-size: .62rem; text-transform: uppercase; letter-spacing: 2px; padding: 16px 10px 7px; }
    .char-toggle { width: 100%; background: var(--bg2); border: none; border-top: 1px solid var(--border); border-radius: 0 0 var(--radius) var(--radius); padding: 6px 12px; display: flex; align-items: center; justify-content: space-between; cursor: pointer; font-family: var(--font-body); font-size: .65rem; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); transition: background .15s, color .15s; }
    .char-toggle:hover { background: var(--bg3); color: var(--accent3); }
    .char-toggle .toggle-icon { transition: transform .3s; }
    .char-toggle.open .toggle-icon { transform: rotate(180deg); }

    /* floating panel */
    .char-panel {
      position: absolute; top: 0; right: calc(100% + 12px);
      width: 400px;
      background: var(--card);
      border: 2px solid var(--accent);
      border-radius: var(--radius);
      box-shadow: -6px 6px 0 rgba(78,94,68,.3), 0 0 0 1px var(--border);
      z-index: 200;
      opacity: 0; pointer-events: none;
      transform: translateX(10px);
      transition: opacity .25s ease, transform .25s ease;
      overflow: hidden;
    }
    .char-panel.open { opacity: 1; pointer-events: auto; transform: translateX(0); }
    .cp-header { background: var(--accent3); color: var(--card); font-family: var(--font-head); font-style: italic; font-size: 1.1rem; padding: 10px 18px; display: flex; align-items: center; justify-content: space-between; border-bottom: 2px solid var(--accent); }
    .cp-header-left { display: flex; align-items: center; gap: 8px; }
    .cp-close { background: none; border: none; cursor: pointer; color: var(--card); opacity: .7; font-size: .85rem; transition: opacity .15s; }
    .cp-close:hover { opacity: 1; }
    .cp-img { width: 100%; height: 140px; background: var(--bg3); background-size: cover; background-position: center; display: flex; align-items: center; justify-content: center; color: var(--muted); font-size: 2.5rem; border-bottom: 1px solid var(--border); }
    .cp-body { padding: 16px 18px 18px; }
    .cp-row { display: flex; gap: 8px; margin-bottom: 10px; align-items: flex-start; }
    .cp-label { font-size: .6rem; text-transform: uppercase; letter-spacing: 1px; color: var(--muted); width: 64px; flex-shrink: 0; padding-top: 2px; }
    .cp-val { font-size: .78rem; line-height: 1.6; color: var(--text); }
    .cp-traits { display: flex; flex-wrap: wrap; gap: 5px; }
    .cp-trait { background: var(--bg2); border-radius: 4px; padding: 3px 9px; font-size: .62rem; text-transform: uppercase; letter-spacing: 1px; color: var(--muted); }
    .cp-link { display: inline-flex; align-items: center; gap: 6px; margin-top: 10px; font-size: .68rem; letter-spacing: 1px; text-transform: uppercase; color: var(--accent3); text-decoration: none; border-bottom: 1px dashed var(--accent2); padding-bottom: 1px; }
    .cp-link:hover { color: var(--warm); border-color: var(--warm); }

    /* ══════════════════════════════════════════
       CREATE MODE
    ══════════════════════════════════════════ */
    #createTrigger {
      position: fixed; bottom: 16px; left: 50%; transform: translateX(-50%);
      background: var(--card); border: 1px solid var(--border);
      border-radius: 20px; padding: 5px 18px;
      font-family: var(--font-body); font-size: .6rem;
      letter-spacing: 2px; text-transform: uppercase; color: var(--muted);
      cursor: pointer; box-shadow: var(--shadow);
      transition: background .15s, color .15s; z-index: 300;
    }
    #createTrigger:hover { background: var(--bg2); color: var(--accent3); }

    #createOverlay { position: fixed; inset: 0; z-index: 400; background: rgba(43,35,24,.55); backdrop-filter: blur(4px); display: none; align-items: center; justify-content: center; }
    #createOverlay.active { display: flex; }

    #createModal { background: var(--card); border: 2px solid var(--accent); border-radius: var(--radius); box-shadow: 8px 8px 0 rgba(78,94,68,.35); width: min(860px,95vw); max-height: 90vh; display: flex; flex-direction: column; overflow: hidden; }

    .cm-header { background: var(--accent3); color: var(--card); font-family: var(--font-head); font-style: italic; font-size: 1.3rem; padding: 14px 22px; display: flex; align-items: center; justify-content: space-between; border-bottom: 2px solid var(--accent); flex-shrink: 0; }
    .cm-close { background: none; border: none; cursor: pointer; color: var(--card); font-size: 1rem; opacity: .8; transition: opacity .15s; }
    .cm-close:hover { opacity: 1; }

    .cm-tabs { display: flex; border-bottom: 1px solid var(--border); flex-shrink: 0; background: var(--bg2); overflow-x: auto; }
    .cm-tab { padding: 9px 16px; border: none; background: none; font-family: var(--font-body); font-size: .65rem; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); cursor: pointer; border-bottom: 2px solid transparent; margin-bottom: -1px; white-space: nowrap; transition: color .15s; }
    .cm-tab:hover { color: var(--accent3); }
    .cm-tab.active { color: var(--accent3); border-bottom-color: var(--accent3); }

    .cm-body { overflow-y: auto; padding: 22px 26px; flex: 1; scrollbar-width: thin; scrollbar-color: var(--accent2) transparent; }
    .cm-pane { display: none; }
    .cm-pane.active { display: block; }

    .cm-row { margin-bottom: 16px; }
    .cm-row label { display: block; font-size: .62rem; text-transform: uppercase; letter-spacing: 1.5px; color: var(--muted); margin-bottom: 5px; }
    .cm-row input[type="text"], .cm-row textarea, .cm-row select { width: 100%; background: var(--bg2); border: 1px solid var(--border); border-radius: 8px; padding: 8px 12px; font-family: var(--font-body); font-size: .8rem; color: var(--text); outline: none; transition: border-color .15s; }
    .cm-row input:focus, .cm-row textarea:focus { border-color: var(--accent); }
    .cm-row textarea { resize: vertical; min-height: 72px; }
    .cm-row select { cursor: pointer; }

    /* toggle switches */
    .cm-toggle-row { display: flex; align-items: center; justify-content: space-between; padding: 9px 0; border-bottom: 1px dashed var(--border); font-size: .78rem; }
    .cm-switch { position: relative; width: 36px; height: 20px; flex-shrink: 0; }
    .cm-switch input { opacity: 0; width: 0; height: 0; }
    .cm-switch .sw-track { position: absolute; inset: 0; border-radius: 20px; background: var(--bg3); cursor: pointer; transition: background .2s; }
    .cm-switch .sw-track::after { content: ''; position: absolute; top: 3px; left: 3px; width: 14px; height: 14px; border-radius: 50%; background: var(--card); transition: left .2s; }
    .cm-switch input:checked + .sw-track { background: var(--accent); }
    .cm-switch input:checked + .sw-track::after { left: 19px; }

    /* colors */
    .color-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
    .color-field { display: flex; align-items: center; gap: 8px; }
    .color-field input[type="color"] { width: 36px; height: 36px; border: 1px solid var(--border); border-radius: 6px; cursor: pointer; background: none; padding: 2px; }
    .color-field span { font-size: .7rem; color: var(--muted); }

    /* char entries */
    .char-entry { background: var(--bg2); border-radius: 10px; padding: 14px; margin-bottom: 14px; border: 1px solid var(--border); }
    .char-entry-head { font-family: var(--font-head); font-style: italic; font-size: 1rem; color: var(--accent3); margin-bottom: 10px; }

    /* fronter entries in create mode */
    .fronter-entry { display: flex; gap: 10px; align-items: flex-start; background: var(--bg2); border-radius: 10px; padding: 12px; margin-bottom: 10px; border: 1px solid var(--border); }
    .fronter-entry-fields { flex: 1; display: flex; flex-direction: column; gap: 8px; }
    .fronter-entry .cm-row { margin: 0; }
    .fe-remove { background: none; border: 1px solid var(--border); border-radius: 6px; padding: 4px 8px; cursor: pointer; font-size: .7rem; color: var(--muted); transition: background .15s, color .15s; flex-shrink: 0; margin-top: 2px; }
    .fe-remove:hover { background: #fde0e0; color: #8b1a1a; border-color: #e0a0a0; }
    .btn-add-fronter { display: flex; align-items: center; gap: 6px; background: var(--bg2); border: 1px dashed var(--border); border-radius: 8px; padding: 8px 14px; cursor: pointer; font-family: var(--font-body); font-size: .68rem; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); width: 100%; justify-content: center; margin-top: 4px; transition: background .15s, color .15s; }
    .btn-add-fronter:hover { background: var(--bg3); color: var(--accent3); }

    /* custom section builder */
    .custom-sec-entry { background: var(--bg2); border-radius: 10px; padding: 14px; margin-bottom: 14px; border: 1px solid var(--border); }
    .cse-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px; }
    .cse-title { font-family: var(--font-head); font-style: italic; font-size: .95rem; color: var(--accent3); }
    .cse-remove { background: none; border: 1px solid var(--border); border-radius: 6px; padding: 3px 8px; cursor: pointer; font-size: .65rem; color: var(--muted); transition: background .15s; }
    .cse-remove:hover { background: #fde0e0; color: #8b1a1a; }
    .btn-add-section { display: flex; align-items: center; gap: 6px; background: var(--bg2); border: 1px dashed var(--border); border-radius: 8px; padding: 10px 14px; cursor: pointer; font-family: var(--font-body); font-size: .68rem; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); width: 100%; justify-content: center; margin-top: 8px; transition: background .15s, color .15s; }
    .btn-add-section:hover { background: var(--bg3); color: var(--accent3); }

    .cm-footer { padding: 14px 26px; border-top: 1px solid var(--border); flex-shrink: 0; display: flex; justify-content: flex-end; gap: 10px; }
    .btn-apply { background: var(--accent3); color: var(--card); border: none; border-radius: 8px; padding: 9px 24px; font-family: var(--font-body); font-size: .72rem; letter-spacing: 1.5px; text-transform: uppercase; cursor: pointer; transition: background .15s; }
    .btn-apply:hover { background: var(--accent); }
    .btn-cancel { background: var(--bg2); color: var(--muted); border: 1px solid var(--border); border-radius: 8px; padding: 9px 18px; font-family: var(--font-body); font-size: .72rem; letter-spacing: 1.5px; text-transform: uppercase; cursor: pointer; transition: background .15s; }
    .btn-cancel:hover { background: var(--bg3); }

    .hidden-section { display: none !important; }
    .sec-head.head-hidden { opacity: 0; height: 0; margin: 0; padding: 0; pointer-events: none; overflow: hidden; box-shadow: none; }

    /* credits */
    .credits { text-align: right; margin-top: 14px; font-size: .62rem; color: var(--muted); }
    .credits a { color: var(--muted); }

    /* responsive */
    @media (max-width: 940px) { .main-grid { grid-template-columns: 220px 1fr; } .sidebar-right { display: none; } }
    @media (max-width: 620px) { .main-grid { grid-template-columns: 1fr; } .sidebar-left { position: static; } .avatar-wrap { margin-top: -55px; } }
  </style>
</head>
<body>

<!-- BANNER -->
<div class="banner"><div id="bannerImg"></div></div>

<div class="page-wrap">
<div class="main-grid">

  <!-- LEFT SIDEBAR -->
  <aside class="sidebar-left">
    <div class="card">
      <div class="left-inner">
        <div class="avatar-wrap">
          <div id="avatarEl"><i class="fa-solid fa-leaf"></i></div>
        </div>
        <h1 class="username" id="displayName">System<em>Name</em></h1>
        <p class="tagline" id="displayTagline">collective · they/them · system</p>

        <div class="slabel"><i class="fa-solid fa-compass fa-xs"></i> Pages</div>
        <ul class="nav-list">
          <li><a href="YOUR_PERSONALITIES_URL" target="_blank"><span><i class="fa-solid fa-masks-theater nav-icon"></i>&nbsp; Personalities</span><i class="fa-solid fa-arrow-up-right-from-square nav-arrow"></i></a></li>
          <li><a href="YOUR_SEARCH_URL" target="_blank"><span><i class="fa-solid fa-magnifying-glass nav-icon"></i>&nbsp; Categorical Search</span><i class="fa-solid fa-arrow-up-right-from-square nav-arrow"></i></a></li>
          <li><a href="YOUR_GUIDE_URL" target="_blank"><span><i class="fa-solid fa-circle-question nav-icon"></i>&nbsp; Uncertainty Guide</span><i class="fa-solid fa-arrow-up-right-from-square nav-arrow"></i></a></li>
          <li><a href="YOUR_COMMS_URL" target="_blank"><span><i class="fa-solid fa-comments nav-icon"></i>&nbsp; Communications Hub</span><i class="fa-solid fa-arrow-up-right-from-square nav-arrow"></i></a></li>
        </ul>

        <div class="slabel"><i class="fa-solid fa-link fa-xs"></i> Socials</div>
        <ul class="social-list">
          <li><a href="LINK_HERE" target="_blank"><i class="fab fa-deviantart"></i> DeviantArt</a></li>
          <li><a href="LINK_HERE" target="_blank"><i class="fas fa-paw"></i> FurAffinity</a></li>
          <li><a href="LINK_HERE" target="_blank"><i class="fab fa-bluesky"></i> Bluesky</a></li>
          <li><a href="LINK_HERE" target="_blank"><i class="fab fa-tumblr"></i> Tumblr</a></li>
          <li><a href="#" target="_blank"><i class="fab fa-discord"></i> system#0000</a></li>
        </ul>
      </div>
    </div>
  </aside>

  <!-- CENTER SCROLL BOX -->
  <main>
    <div class="scroll-card">

      <!-- Music bar -->
      <div class="music-bar" id="musicBarSection">
        <iframe class="yt-embed" src="https://www.youtube.com/embed/dQw4w9WgXcQ?autoplay=0&controls=0" title="bg music" allow="autoplay"></iframe>
        <i class="fa-solid fa-music"></i>
        <div class="ticker-wrap"><span id="musicTicker">♪ &nbsp;song title — artist &nbsp;&nbsp;&nbsp;&nbsp; ♪ &nbsp;song title — artist &nbsp;&nbsp;&nbsp;&nbsp; ♪ &nbsp;song title — artist &nbsp;&nbsp;&nbsp;&nbsp;</span></div>
      </div>

      <div class="scroll-inner" id="scrollInner">

        <!-- ABOUT US -->
        <div id="aboutSection">
          <div class="sec-head" id="aboutHead"><i class="fa-solid fa-seedling"></i> About Us</div>
          <p class="prose" id="aboutText">Write about your system here — your collective identity, how you communicate, what you'd like visitors to know first. This box scrolls so take as much space as you need.</p>
          <p class="prose" id="aboutText2">Second paragraph if you'd like. A note on interaction preferences, a friendly hello, or anything that feels right.</p>
          <div class="tz-widget" id="tzWidget">
            <i class="fa-solid fa-clock"></i>
            <div class="tz-right">
              <div class="tz-label">Our Timezone</div>
              <div id="tzName">—</div>
              <div id="tzClock">--:--</div>
            </div>
          </div>
        </div>

        <div class="divider" id="div1"></div>

        <!-- FREQUENT FRONTERS -->
        <div id="frontersSection">
          <div class="sec-head" id="frontersHead"><i class="fa-solid fa-users"></i> Frequent Fronters</div>
          <div class="slideshow" id="slideshow">
            <div class="slide active" style="background-image: url('');"><div class="slide-caption">fronter name · role</div></div>
            <div class="slide" style="background-image: url('');"><div class="slide-caption">fronter name · role</div></div>
            <div class="slide" style="background-image: url('');"><div class="slide-caption">fronter name · role</div></div>
            <div class="slide-ph" id="slidePh"><i class="fa-regular fa-image"></i>Add fronter image URLs in Create Mode</div>
          </div>
          <div class="slide-dots" id="slideDots">
            <div class="dot active" onclick="goSlide(0)"></div>
            <div class="dot" onclick="goSlide(1)"></div>
            <div class="dot" onclick="goSlide(2)"></div>
          </div>
          <!-- Fronter profile cards grid -->
          <div class="fronter-grid" id="fronterGrid">
            <!-- populated by JS / Create Mode -->
            <div class="fronter-card fronter-add" onclick="openCreate('tabFronters')" title="Add fronter in Create Mode">
              <i class="fa-solid fa-plus"></i>
              <span>Add</span>
            </div>
          </div>
        </div>

        <div class="divider" id="div2"></div>

        <!-- DISCLAIMER -->
        <div id="disclaimerSection">
          <div class="sec-head" id="disclaimerHead"><i class="fa-solid fa-circle-info"></i> Disclaimer</div>
          <div class="disclaimer-box">
            <ul id="disclaimerList">
              <li>Write your first disclaimer or interaction note here.</li>
              <li>Add content warnings, DNI criteria, or system-specific communication notes.</li>
              <li>Preferred contact methods, comfort topics, or anything visitors should know before reaching out.</li>
            </ul>
          </div>
        </div>

        <!-- CUSTOM SECTIONS MOUNT POINT -->
        <div id="customSectionsMount"></div>

      </div><!-- /scroll-inner -->
    </div>
  </main>

  <!-- RIGHT SIDEBAR — character cards -->
  <aside class="sidebar-right">

    <div class="char-card" id="card1">
      <div class="char-thumb" id="ct1" style="background-image: url('');"><i class="fa-solid fa-paw"></i><div class="char-thumb-overlay"></div><div class="char-thumb-name" id="cname1">Member Name</div></div>
      <button class="char-toggle" onclick="togglePanel('panel1',this)" aria-expanded="false"><span>View Info</span><i class="fa-solid fa-chevron-down toggle-icon"></i></button>
      <div class="char-panel" id="panel1">
        <div class="cp-header"><div class="cp-header-left"><i class="fa-solid fa-id-card"></i> <span id="cpname1">Member Name</span></div><button class="cp-close" onclick="closePanel('panel1')"><i class="fa-solid fa-xmark"></i></button></div>
        <div class="cp-img" id="cpimg1" style="background-image: url('');"><i class="fa-solid fa-paw"></i></div>
        <div class="cp-body">
          <div class="cp-row"><span class="cp-label">Species</span><span class="cp-val" id="cpspecies1">Species placeholder</span></div>
          <div class="cp-row"><span class="cp-label">Bio</span><span class="cp-val" id="cpbio1">A short description of this member — their role, personality, how they communicate.</span></div>
          <div class="cp-row"><span class="cp-label">Traits</span><div class="cp-traits" id="cptraits1"><span class="cp-trait">trait one</span><span class="cp-trait">trait two</span><span class="cp-trait">trait three</span></div></div>
          <a href="#" target="_blank" class="cp-link" id="cplink1"><i class="fa-solid fa-arrow-up-right-from-square fa-xs"></i> Full page</a>
        </div>
      </div>
    </div>

    <div class="char-card" id="card2">
      <div class="char-thumb" id="ct2" style="background-image: url('');"><i class="fa-solid fa-paw"></i><div class="char-thumb-overlay"></div><div class="char-thumb-name" id="cname2">Member Name</div></div>
      <button class="char-toggle" onclick="togglePanel('panel2',this)" aria-expanded="false"><span>View Info</span><i class="fa-solid fa-chevron-down toggle-icon"></i></button>
      <div class="char-panel" id="panel2">
        <div class="cp-header"><div class="cp-header-left"><i class="fa-solid fa-id-card"></i> <span id="cpname2">Member Name</span></div><button class="cp-close" onclick="closePanel('panel2')"><i class="fa-solid fa-xmark"></i></button></div>
        <div class="cp-img" id="cpimg2" style="background-image: url('');"><i class="fa-solid fa-paw"></i></div>
        <div class="cp-body">
          <div class="cp-row"><span class="cp-label">Species</span><span class="cp-val" id="cpspecies2">Species placeholder</span></div>
          <div class="cp-row"><span class="cp-label">Bio</span><span class="cp-val" id="cpbio2">A short description of this member.</span></div>
          <div class="cp-row"><span class="cp-label">Traits</span><div class="cp-traits" id="cptraits2"><span class="cp-trait">trait one</span><span class="cp-trait">trait two</span><span class="cp-trait">trait three</span></div></div>
          <a href="#" target="_blank" class="cp-link" id="cplink2"><i class="fa-solid fa-arrow-up-right-from-square fa-xs"></i> Full page</a>
        </div>
      </div>
    </div>

    <div class="char-card" id="card3">
      <div class="char-thumb" id="ct3" style="background-image: url('');"><i class="fa-solid fa-paw"></i><div class="char-thumb-overlay"></div><div class="char-thumb-name" id="cname3">Member Name</div></div>
      <button class="char-toggle" onclick="togglePanel('panel3',this)" aria-expanded="false"><span>View Info</span><i class="fa-solid fa-chevron-down toggle-icon"></i></button>
      <div class="char-panel" id="panel3">
        <div class="cp-header"><div class="cp-header-left"><i class="fa-solid fa-id-card"></i> <span id="cpname3">Member Name</span></div><button class="cp-close" onclick="closePanel('panel3')"><i class="fa-solid fa-xmark"></i></button></div>
        <div class="cp-img" id="cpimg3" style="background-image: url('');"><i class="fa-solid fa-paw"></i></div>
        <div class="cp-body">
          <div class="cp-row"><span class="cp-label">Species</span><span class="cp-val" id="cpspecies3">Species placeholder</span></div>
          <div class="cp-row"><span class="cp-label">Bio</span><span class="cp-val" id="cpbio3">A short description of this member.</span></div>
          <div class="cp-row"><span class="cp-label">Traits</span><div class="cp-traits" id="cptraits3"><span class="cp-trait">trait one</span><span class="cp-trait">trait two</span><span class="cp-trait">trait three</span></div></div>
          <a href="#" target="_blank" class="cp-link" id="cplink3"><i class="fa-solid fa-arrow-up-right-from-square fa-xs"></i> Full page</a>
        </div>
      </div>
    </div>

  </aside>
</div><!-- /main-grid -->

<div class="credits">layout by you &nbsp;·&nbsp; <a href="https://toyhou.se/37213301.0-carbonate" target="_blank">carbonate base by teiyoq</a></div>
</div><!-- /page-wrap -->

<!-- CREATE MODE TRIGGER -->
<button id="createTrigger" onclick="openCreate()"><i class="fa-solid fa-pen-to-square"></i> &nbsp;create mode</button>

<!-- CREATE MODE MODAL -->
<div id="createOverlay">
<div id="createModal">
  <div class="cm-header">
    <span><i class="fa-solid fa-wand-magic-sparkles"></i> &nbsp;Create Mode</span>
    <button class="cm-close" onclick="closeCreate()"><i class="fa-solid fa-xmark"></i></button>
  </div>
  <div class="cm-tabs">
    <button class="cm-tab active" data-tab="tabText" onclick="switchTab(this)">Text</button>
    <button class="cm-tab" data-tab="tabFronters" onclick="switchTab(this)">Fronters</button>
    <button class="cm-tab" data-tab="tabSections" onclick="switchTab(this)">Custom Sections</button>
    <button class="cm-tab" data-tab="tabColors" onclick="switchTab(this)">Colors</button>
    <button class="cm-tab" data-tab="tabImages" onclick="switchTab(this)">Images</button>
    <button class="cm-tab" data-tab="tabVisibility" onclick="switchTab(this)">Visibility</button>
  </div>
  <div class="cm-body">

    <!-- TAB: TEXT -->
    <div class="cm-pane active" id="tabText">
      <div class="cm-row"><label>System / Display Name (wrap a word in *asterisks* for italic)</label><input type="text" id="cm_name" placeholder="System*Name*"></div>
      <div class="cm-row"><label>Tagline</label><input type="text" id="cm_tagline" placeholder="collective · they/them · system"></div>
      <div class="cm-row"><label>About Us — Paragraph 1</label><textarea id="cm_about1"></textarea></div>
      <div class="cm-row"><label>About Us — Paragraph 2</label><textarea id="cm_about2"></textarea></div>
      <div class="cm-row"><label>Timezone string (e.g. America/Chicago)</label><input type="text" id="cm_tz" placeholder="America/Chicago"></div>
      <div class="cm-row"><label>Timezone Label (shown to visitors)</label><input type="text" id="cm_tzLabel" placeholder="Central Time (CT)"></div>
      <div class="cm-row"><label>Music Ticker Text</label><input type="text" id="cm_ticker" placeholder="♪ song title — artist"></div>
      <div class="cm-row"><label>Disclaimer — one item per line</label><textarea id="cm_disclaimer" rows="5" placeholder="Line 1&#10;Line 2&#10;Line 3"></textarea></div>
      <hr style="border:none;border-top:1px dashed var(--border);margin:16px 0">
      <div style="font-size:.65rem;text-transform:uppercase;letter-spacing:2px;color:var(--muted);margin-bottom:12px;">Sidebar Member Cards</div>
      <div class="char-entry">
        <div class="char-entry-head">Card 1</div>
        <div class="cm-row"><label>Name</label><input type="text" id="cm_c1name"></div>
        <div class="cm-row"><label>Species</label><input type="text" id="cm_c1species"></div>
        <div class="cm-row"><label>Bio</label><textarea id="cm_c1bio" rows="2"></textarea></div>
        <div class="cm-row"><label>Traits (comma-separated)</label><input type="text" id="cm_c1traits"></div>
        <div class="cm-row"><label>Full Page URL</label><input type="text" id="cm_c1link"></div>
      </div>
      <div class="char-entry">
        <div class="char-entry-head">Card 2</div>
        <div class="cm-row"><label>Name</label><input type="text" id="cm_c2name"></div>
        <div class="cm-row"><label>Species</label><input type="text" id="cm_c2species"></div>
        <div class="cm-row"><label>Bio</label><textarea id="cm_c2bio" rows="2"></textarea></div>
        <div class="cm-row"><label>Traits (comma-separated)</label><input type="text" id="cm_c2traits"></div>
        <div class="cm-row"><label>Full Page URL</label><input type="text" id="cm_c2link"></div>
      </div>
      <div class="char-entry">
        <div class="char-entry-head">Card 3</div>
        <div class="cm-row"><label>Name</label><input type="text" id="cm_c3name"></div>
        <div class="cm-row"><label>Species</label><input type="text" id="cm_c3species"></div>
        <div class="cm-row"><label>Bio</label><textarea id="cm_c3bio" rows="2"></textarea></div>
        <div class="cm-row"><label>Traits (comma-separated)</label><input type="text" id="cm_c3traits"></div>
        <div class="cm-row"><label>Full Page URL</label><input type="text" id="cm_c3link"></div>
      </div>
    </div>

    <!-- TAB: FRONTERS -->
    <div class="cm-pane" id="tabFronters">
      <p style="font-size:.76rem;color:var(--muted);margin-bottom:16px;line-height:1.7">Add profile cards for frequent fronters. Each card links to their (future) profile page and shows a portrait.</p>
      <div id="fronterEntries"></div>
      <button class="btn-add-fronter" onclick="addFronterEntry()"><i class="fa-solid fa-plus"></i> Add Fronter</button>
    </div>

    <!-- TAB: CUSTOM SECTIONS -->
    <div class="cm-pane" id="tabSections">
      <p style="font-size:.76rem;color:var(--muted);margin-bottom:16px;line-height:1.7">Build additional sections to append after the Disclaimer. Each section has an optional header bar and supports prose, bullets, an image, or a link list.</p>
      <div id="customSecEntries"></div>
      <button class="btn-add-section" onclick="addCustomSection()"><i class="fa-solid fa-plus"></i> Add Section</button>
    </div>

    <!-- TAB: COLORS -->
    <div class="cm-pane" id="tabColors">
      <p style="font-size:.76rem;color:var(--muted);margin-bottom:16px;line-height:1.7">Adjust the color tokens. Changes apply on click of Apply.</p>
      <div class="color-grid">
        <div class="cm-row"><label>Page Background</label><div class="color-field"><input type="color" id="cc_bg" value="#f2ede6"><span>--bg</span></div></div>
        <div class="cm-row"><label>Card Surface</label><div class="color-field"><input type="color" id="cc_card" value="#faf8f4"><span>--card</span></div></div>
        <div class="cm-row"><label>Accent (sage)</label><div class="color-field"><input type="color" id="cc_accent" value="#7a8c6e"><span>--accent</span></div></div>
        <div class="cm-row"><label>Accent Dark</label><div class="color-field"><input type="color" id="cc_accent3" value="#4e5e44"><span>--accent3</span></div></div>
        <div class="cm-row"><label>Warm Accent</label><div class="color-field"><input type="color" id="cc_warm" value="#c4956a"><span>--warm</span></div></div>
        <div class="cm-row"><label>Text Color</label><div class="color-field"><input type="color" id="cc_text" value="#2b2318"><span>--text</span></div></div>
      </div>
    </div>

    <!-- TAB: IMAGES -->
    <div class="cm-pane" id="tabImages">
      <p style="font-size:.76rem;color:var(--muted);margin-bottom:16px;line-height:1.7">Paste hosted image URLs (Toyhouse, Imgur, Discord CDN, etc.).</p>
      <div class="cm-row"><label>Banner Image URL</label><input type="text" id="cm_banner" placeholder="https://..."></div>
      <div class="cm-row"><label>Avatar Image URL</label><input type="text" id="cm_avatar" placeholder="https://..."></div>
      <hr style="border:none;border-top:1px dashed var(--border);margin:14px 0">
      <div style="font-size:.65rem;text-transform:uppercase;letter-spacing:2px;color:var(--muted);margin-bottom:12px;">Slideshow Images</div>
      <div class="cm-row"><label>Slide 1 — Image URL</label><input type="text" id="cm_s1" placeholder="https://..."></div>
      <div class="cm-row"><label>Slide 1 — Caption</label><input type="text" id="cm_s1cap" placeholder="fronter name · role"></div>
      <div class="cm-row"><label>Slide 2 — Image URL</label><input type="text" id="cm_s2" placeholder="https://..."></div>
      <div class="cm-row"><label>Slide 2 — Caption</label><input type="text" id="cm_s2cap" placeholder="fronter name · role"></div>
      <div class="cm-row"><label>Slide 3 — Image URL</label><input type="text" id="cm_s3" placeholder="https://..."></div>
      <div class="cm-row"><label>Slide 3 — Caption</label><input type="text" id="cm_s3cap" placeholder="fronter name · role"></div>
      <hr style="border:none;border-top:1px dashed var(--border);margin:14px 0">
      <div style="font-size:.65rem;text-transform:uppercase;letter-spacing:2px;color:var(--muted);margin-bottom:12px;">Sidebar Card Images</div>
      <div class="cm-row"><label>Card 1 Image URL</label><input type="text" id="cm_ci1" placeholder="https://..."></div>
      <div class="cm-row"><label>Card 2 Image URL</label><input type="text" id="cm_ci2" placeholder="https://..."></div>
      <div class="cm-row"><label>Card 3 Image URL</label><input type="text" id="cm_ci3" placeholder="https://..."></div>
    </div>

    <!-- TAB: VISIBILITY -->
    <div class="cm-pane" id="tabVisibility">
      <p style="font-size:.76rem;color:var(--muted);margin-bottom:14px;line-height:1.7">Show or hide each section and its header bar independently.</p>
      <div style="font-size:.62rem;text-transform:uppercase;letter-spacing:2px;color:var(--muted);margin:10px 0 6px">Section Visibility</div>
      <div class="cm-toggle-row"><span>Music Bar</span><label class="cm-switch"><input type="checkbox" id="tog_music" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>About Us — section</span><label class="cm-switch"><input type="checkbox" id="tog_about" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>About Us — header bar</span><label class="cm-switch"><input type="checkbox" id="tog_aboutHead" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Timezone Widget</span><label class="cm-switch"><input type="checkbox" id="tog_tz" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Frequent Fronters — section</span><label class="cm-switch"><input type="checkbox" id="tog_fronters" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Frequent Fronters — header bar</span><label class="cm-switch"><input type="checkbox" id="tog_frontersHead" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Disclaimer — section</span><label class="cm-switch"><input type="checkbox" id="tog_disclaimer" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Disclaimer — header bar</span><label class="cm-switch"><input type="checkbox" id="tog_disclaimerHead" checked><span class="sw-track"></span></label></div>
      <div style="font-size:.62rem;text-transform:uppercase;letter-spacing:2px;color:var(--muted);margin:16px 0 6px">Sidebar Cards</div>
      <div class="cm-toggle-row"><span>Member Card 1</span><label class="cm-switch"><input type="checkbox" id="tog_card1" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Member Card 2</span><label class="cm-switch"><input type="checkbox" id="tog_card2" checked><span class="sw-track"></span></label></div>
      <div class="cm-toggle-row"><span>Member Card 3</span><label class="cm-switch"><input type="checkbox" id="tog_card3" checked><span class="sw-track"></span></label></div>
    </div>

  </div><!-- /cm-body -->
  <div class="cm-footer">
    <button class="btn-cancel" onclick="closeCreate()">Cancel</button>
    <button class="btn-apply" onclick="applyCreate()"><i class="fa-solid fa-check"></i> &nbsp;Apply</button>
  </div>
</div>
</div>

<script>
/* ════════════════════════════════════
   TIMEZONE CLOCK (no seconds)
════════════════════════════════════ */
let tzString = Intl.DateTimeFormat().resolvedOptions().timeZone;
let tzLabel  = tzString;
function updateClock() {
  try {
    document.getElementById('tzName').textContent = tzLabel || tzString;
    document.getElementById('tzClock').textContent =
      new Date().toLocaleTimeString('en-US',{timeZone:tzString,hour12:true,hour:'2-digit',minute:'2-digit'});
  } catch(e) {
    document.getElementById('tzClock').textContent = new Date().toLocaleTimeString('en-US',{hour:'2-digit',minute:'2-digit'});
  }
}
updateClock();
setInterval(updateClock, 10000);

/* ════════════════════════════════════
   SLIDESHOW
════════════════════════════════════ */
let cur = 0;
const slides    = () => document.querySelectorAll('#slideshow .slide');
const dotEls    = () => document.querySelectorAll('#slideDots .dot');
const slidePh   = document.getElementById('slidePh');

function checkImages() {
  const hasImgs = [...slides()].some(s => {
    const bg = s.style.backgroundImage;
    return bg && bg !== "url('')" && bg !== 'url("")';
  });
  if (slidePh) slidePh.style.display = hasImgs ? 'none' : 'flex';
  return hasImgs;
}

function goSlide(n) {
  const sl = slides(); const dt = dotEls();
  if (!sl.length) return;
  sl[cur] && sl[cur].classList.remove('active');
  dt[cur] && dt[cur].classList.remove('active');
  cur = n % sl.length;
  sl[cur] && sl[cur].classList.add('active');
  dt[cur] && dt[cur].classList.add('active');
}

checkImages();
setInterval(() => { if (checkImages()) goSlide(cur + 1); }, 4000);

/* ════════════════════════════════════
   CHARACTER PANEL
════════════════════════════════════ */
function togglePanel(id, btn) {
  const panel = document.getElementById(id);
  const isOpen = panel.classList.contains('open');
  document.querySelectorAll('.char-panel').forEach(p => p.classList.remove('open'));
  document.querySelectorAll('.char-toggle').forEach(b => { b.classList.remove('open'); b.setAttribute('aria-expanded','false'); b.querySelector('span').textContent='View Info'; });
  if (!isOpen) {
    panel.classList.add('open');
    btn.classList.add('open');
    btn.setAttribute('aria-expanded','true');
    btn.querySelector('span').textContent='Hide Info';
  }
}
function closePanel(id) {
  document.getElementById(id).classList.remove('open');
  document.querySelectorAll('.char-toggle').forEach(b => { b.classList.remove('open'); b.querySelector('span').textContent='View Info'; });
}
document.addEventListener('click', e => {
  if (!e.target.closest('.char-card') && !e.target.closest('#createOverlay'))
    document.querySelectorAll('.char-panel').forEach(p => p.classList.remove('open'));
});

/* ════════════════════════════════════
   FRONTER CARDS (live data store)
════════════════════════════════════ */
let fronterData = []; // { name, imgUrl, pageUrl }

function renderFronterGrid() {
  const grid = document.getElementById('fronterGrid');
  const addBtn = `<div class="fronter-card fronter-add" onclick="openCreate('tabFronters')" title="Add fronter in Create Mode"><i class="fa-solid fa-plus"></i><span>Add</span></div>`;
  grid.innerHTML = fronterData.map((f,i) => `
    <a class="fronter-card" href="${f.pageUrl||'#'}" ${f.pageUrl ? 'target="_blank"' : ''} title="${f.name}">
      <div class="fronter-thumb" style="background-image:url('${f.imgUrl||''}')">
        ${f.imgUrl ? '' : '<i class="fa-solid fa-user"></i>'}
      </div>
      <div class="fronter-name">${f.name}</div>
    </a>`).join('') + addBtn;
}

/* ════════════════════════════════════
   CUSTOM SECTIONS (live store)
════════════════════════════════════ */
let customSections = []; // { id, title, showHead, type, content }
let sectionIdCounter = 0;

function renderCustomSections() {
  const mount = document.getElementById('customSectionsMount');
  mount.innerHTML = customSections.map(sec => {
    let bodyHTML = '';
    if (sec.type === 'prose') {
      bodyHTML = `<p class="prose">${sec.content.replace(/\n/g,'<br>')}</p>`;
    } else if (sec.type === 'bullets') {
      const items = sec.content.split('\n').filter(l=>l.trim());
      bodyHTML = `<ul style="padding-left:18px;font-size:.82rem;line-height:1.85">${items.map(i=>`<li>${i}</li>`).join('')}</ul>`;
    } else if (sec.type === 'image') {
      const parts = sec.content.split('\n');
      const url   = parts[0]||'';
      const cap   = parts[1]||'';
      bodyHTML = url ? `<div class="custom-img-block"><img src="${url}" alt="${cap}" loading="lazy">${cap?`<div class="custom-img-caption">${cap}</div>`:''}</div>` : `<p class="prose" style="color:var(--muted)">Add an image URL in Create Mode.</p>`;
    } else if (sec.type === 'links') {
      const lines = sec.content.split('\n').filter(l=>l.trim());
      const items = lines.map(l => { const [label,...rest]=l.split('|'); const url=rest.join('|').trim(); return `<li><a href="${url||'#'}" ${url?'target="_blank"':''}><i class="fa-solid fa-arrow-up-right-from-square fa-xs"></i> ${label.trim()}</a></li>`; }).join('');
      bodyHTML = `<ul class="custom-link-list">${items}</ul>`;
    }
    return `<div class="divider"></div><div class="custom-section" data-sec-id="${sec.id}"><div class="sec-head ${sec.showHead?'':'sec-head head-hidden'}"><i class="fa-solid fa-bookmark"></i> ${sec.title||'Section'}</div><div class="sec-block-inner">${bodyHTML}</div></div>`;
  }).join('');
}

/* ════════════════════════════════════
   CREATE MODE
════════════════════════════════════ */
let fronterEntryCount = 0;
let customSecCount    = 0;

function openCreate(tabId) {
  // pre-fill text fields
  document.getElementById('cm_name').value    = document.getElementById('displayName').innerText;
  document.getElementById('cm_tagline').value = document.getElementById('displayTagline').innerText;
  document.getElementById('cm_about1').value  = document.getElementById('aboutText').innerText.trim();
  document.getElementById('cm_about2').value  = document.getElementById('aboutText2').innerText.trim();
  document.getElementById('cm_tz').value      = tzString;
  document.getElementById('cm_tzLabel').value = tzLabel;
  document.getElementById('cm_ticker').value  = document.getElementById('musicTicker').innerText.replace(/♪.*$/,'').trim() || document.getElementById('musicTicker').innerText;
  document.getElementById('cm_disclaimer').value = [...document.querySelectorAll('#disclaimerList li')].map(l=>l.innerText).join('\n');

  // render fronter entries
  renderFronterEntries();
  // render custom section entries
  renderCustomSecEntries();

  document.getElementById('createOverlay').classList.add('active');
  if (tabId) {
    const btn = document.querySelector(`[data-tab="${tabId}"]`);
    if (btn) switchTab(btn);
  }
}
function closeCreate() { document.getElementById('createOverlay').classList.remove('active'); }

function switchTab(btn) {
  document.querySelectorAll('.cm-pane').forEach(p=>p.classList.remove('active'));
  document.querySelectorAll('.cm-tab').forEach(t=>t.classList.remove('active'));
  document.getElementById(btn.dataset.tab).classList.add('active');
  btn.classList.add('active');
}

/* ── Fronter entries in modal ── */
function renderFronterEntries() {
  const cont = document.getElementById('fronterEntries');
  cont.innerHTML = fronterData.map((f,i)=>`
    <div class="fronter-entry" id="fe_${i}">
      <div class="fronter-entry-fields">
        <div class="cm-row"><label>Name</label><input type="text" id="fe_name_${i}" value="${f.name||''}"></div>
        <div class="cm-row"><label>Portrait Image URL</label><input type="text" id="fe_img_${i}" value="${f.imgUrl||''}"></div>
        <div class="cm-row"><label>Profile Page URL (can be a placeholder)</label><input type="text" id="fe_url_${i}" value="${f.pageUrl||''}"></div>
      </div>
      <button class="fe-remove" onclick="removeFronter(${i})"><i class="fa-solid fa-xmark"></i></button>
    </div>`).join('');
}

function addFronterEntry() {
  fronterData.push({name:'New Fronter',imgUrl:'',pageUrl:''});
  renderFronterEntries();
}
function removeFronter(i) {
  fronterData.splice(i,1);
  renderFronterEntries();
}
function saveFronterEntries() {
  fronterData = fronterData.map((_,i)=>({
    name:    (document.getElementById(`fe_name_${i}`)?.value||'').trim(),
    imgUrl:  (document.getElementById(`fe_img_${i}`)?.value||'').trim(),
    pageUrl: (document.getElementById(`fe_url_${i}`)?.value||'').trim(),
  })).filter(f=>f.name);
}

/* ── Custom section entries in modal ── */
function renderCustomSecEntries() {
  const cont = document.getElementById('customSecEntries');
  cont.innerHTML = customSections.map((sec,i)=>`
    <div class="custom-sec-entry" id="cse_${i}">
      <div class="cse-head">
        <div class="cse-title">Section ${i+1}</div>
        <button class="cse-remove" onclick="removeCustomSec(${i})"><i class="fa-solid fa-xmark"></i> Remove</button>
      </div>
      <div class="cm-row"><label>Section Title</label><input type="text" id="cse_title_${i}" value="${sec.title||''}"></div>
      <div class="cm-row">
        <label>Show header bar?</label>
        <label class="cm-switch"><input type="checkbox" id="cse_head_${i}" ${sec.showHead?'checked':''}><span class="sw-track"></span></label>
      </div>
      <div class="cm-row">
        <label>Content Type</label>
        <select id="cse_type_${i}" onchange="updateSecTypeHint(${i})">
          <option value="prose" ${sec.type==='prose'?'selected':''}>Prose / free text</option>
          <option value="bullets" ${sec.type==='bullets'?'selected':''}>Bullet list (one item per line)</option>
          <option value="image" ${sec.type==='image'?'selected':''}>Image + caption (URL on line 1, caption on line 2)</option>
          <option value="links" ${sec.type==='links'?'selected':''}>Link list (Label | URL, one per line)</option>
        </select>
      </div>
      <div class="cm-row"><label id="cse_hint_${i}">Content</label><textarea id="cse_content_${i}" rows="4">${sec.content||''}</textarea></div>
    </div>`).join('');
}

function updateSecTypeHint(i) {
  const type  = document.getElementById(`cse_type_${i}`).value;
  const hints = { prose:'Write your text here.', bullets:'One bullet item per line.', image:'Line 1: image URL\nLine 2: caption text', links:'Label | https://url.com\nOne link per line' };
  document.getElementById(`cse_hint_${i}`).textContent = hints[type] || 'Content';
}

function addCustomSection() {
  customSections.push({id: ++sectionIdCounter, title:'New Section', showHead:true, type:'prose', content:''});
  renderCustomSecEntries();
}
function removeCustomSec(i) {
  customSections.splice(i,1);
  renderCustomSecEntries();
}
function saveCustomSections() {
  customSections = customSections.map((_,i)=>({
    id:       customSections[i].id,
    title:    (document.getElementById(`cse_title_${i}`)?.value||'').trim() || 'Section',
    showHead: document.getElementById(`cse_head_${i}`)?.checked ?? true,
    type:     document.getElementById(`cse_type_${i}`)?.value || 'prose',
    content:  (document.getElementById(`cse_content_${i}`)?.value||'').trim(),
  }));
}

/* ── APPLY ── */
function applyCreate() {
  // text
  const rawName = document.getElementById('cm_name').value.trim();
  if (rawName) document.getElementById('displayName').innerHTML = rawName.replace(/\*([^*]+)\*/g,'<em>$1</em>');
  const tl = document.getElementById('cm_tagline').value.trim();
  if (tl) document.getElementById('displayTagline').innerText = tl;
  const a1 = document.getElementById('cm_about1').value.trim();
  if (a1) document.getElementById('aboutText').innerText = a1;
  const a2 = document.getElementById('cm_about2').value.trim();
  if (a2) document.getElementById('aboutText2').innerText = a2;

  const tz = document.getElementById('cm_tz').value.trim();
  if (tz) tzString = tz;
  const tzl = document.getElementById('cm_tzLabel').value.trim();
  if (tzl) tzLabel = tzl;
  updateClock();

  const ticker = document.getElementById('cm_ticker').value.trim();
  if (ticker) { const t=ticker+' &nbsp;&nbsp;&nbsp;&nbsp; '+ticker+' &nbsp;&nbsp;&nbsp;&nbsp; '+ticker; document.getElementById('musicTicker').innerHTML=t; }

  const disc = document.getElementById('cm_disclaimer').value.trim();
  if (disc) document.getElementById('disclaimerList').innerHTML = disc.split('\n').filter(l=>l.trim()).map(l=>`<li>${l}</li>`).join('');

  // member cards
  [1,2,3].forEach(n=>{
    const name    = document.getElementById(`cm_c${n}name`)?.value.trim();
    const species = document.getElementById(`cm_c${n}species`)?.value.trim();
    const bio     = document.getElementById(`cm_c${n}bio`)?.value.trim();
    const traits  = document.getElementById(`cm_c${n}traits`)?.value.trim();
    const link    = document.getElementById(`cm_c${n}link`)?.value.trim();
    if (name)    { document.getElementById(`cname${n}`).innerText=name; document.getElementById(`cpname${n}`).innerText=name; }
    if (species) document.getElementById(`cpspecies${n}`).innerText=species;
    if (bio)     document.getElementById(`cpbio${n}`).innerText=bio;
    if (traits)  document.getElementById(`cptraits${n}`).innerHTML=traits.split(',').map(t=>`<span class="cp-trait">${t.trim()}</span>`).join('');
    if (link)    document.getElementById(`cplink${n}`).href=link;
  });

  // colors
  const cmap = {cc_bg:'--bg',cc_card:'--card',cc_accent:'--accent',cc_accent3:'--accent3',cc_warm:'--warm',cc_text:'--text'};
  Object.entries(cmap).forEach(([id,v])=>document.documentElement.style.setProperty(v,document.getElementById(id).value));
  const bg2 = lightenHex(document.getElementById('cc_bg').value,-8);
  document.documentElement.style.setProperty('--bg2', bg2);
  document.documentElement.style.setProperty('--bg3', lightenHex(document.getElementById('cc_bg').value,-16));

  // images
  const banner = document.getElementById('cm_banner').value.trim();
  if (banner) document.getElementById('bannerImg').style.backgroundImage=`url('${banner}')`;
  const avatar = document.getElementById('cm_avatar').value.trim();
  if (avatar) document.getElementById('avatarEl').innerHTML=`<img src="${avatar}" alt="avatar">`;

  [1,2,3].forEach(n=>{
    const s = document.getElementById(`cm_s${n}`)?.value.trim();
    const c = document.getElementById(`cm_s${n}cap`)?.value.trim();
    const sl = document.querySelectorAll('#slideshow .slide')[n-1];
    if (s && sl) sl.style.backgroundImage=`url('${s}')`;
    if (c && sl) sl.querySelector('.slide-caption').innerText=c;
    const ci = document.getElementById(`cm_ci${n}`)?.value.trim();
    if (ci) { document.getElementById(`ct${n}`).style.backgroundImage=`url('${ci}')`; document.getElementById(`cpimg${n}`).style.backgroundImage=`url('${ci}')`; }
  });
  checkImages();

  // fronters
  saveFronterEntries();
  renderFronterGrid();

  // custom sections
  saveCustomSections();
  renderCustomSections();

  // visibility
  const secMap = { tog_music:'musicBarSection', tog_about:'aboutSection', tog_fronters:'frontersSection', tog_disclaimer:'disclaimerSection', tog_card1:'card1', tog_card2:'card2', tog_card3:'card3' };
  Object.entries(secMap).forEach(([t,s])=>{ const el=document.getElementById(s); if(el) el.classList.toggle('hidden-section',!document.getElementById(t).checked); });
  // head toggles
  const headMap = { tog_aboutHead:'aboutHead', tog_frontersHead:'frontersHead', tog_disclaimerHead:'disclaimerHead' };
  Object.entries(headMap).forEach(([t,h])=>{ const el=document.getElementById(h); if(el) el.classList.toggle('head-hidden',!document.getElementById(t).checked); });
  // timezone widget
  document.getElementById('tzWidget').classList.toggle('hidden-section',!document.getElementById('tog_tz').checked);

  closeCreate();
}

/* helper: lighten/darken hex */
function lightenHex(hex, amt) {
  const n = parseInt(hex.replace('#',''),16);
  let r=Math.min(255,Math.max(0,((n>>16)&255)+amt));
  let g=Math.min(255,Math.max(0,((n>>8)&255)+amt));
  let b=Math.min(255,Math.max(0,(n&255)+amt));
  return '#'+[r,g,b].map(x=>x.toString(16).padStart(2,'0')).join('');
}

// close overlay on backdrop
document.getElementById('createOverlay').addEventListener('click', e=>{ if(e.target===document.getElementById('createOverlay')) closeCreate(); });

// init grid
renderFronterGrid();
</script>
</body>
</html>
